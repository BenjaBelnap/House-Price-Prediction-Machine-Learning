# Requirements for building standalone executable
# These are in addition to the main project requirements

# Core building tool
pyinstaller>=5.0

# Additional runtime requirements that may be needed for the executable
# (Most should already be in your main project requirements)
uvicorn[standard]>=0.18.0
fastapi>=0.68.0
pandas>=1.3.0
numpy>=1.21.0
scikit-learn>=1.0.0
xgboost>=1.5.0
joblib>=1.1.0
pydantic>=1.8.0

# Additional dependencies that might be needed for packaging
setuptools>=60.0.0
wheel>=0.37.0
