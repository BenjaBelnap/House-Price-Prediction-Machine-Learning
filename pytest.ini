[pytest]
minversion = 7.0
addopts = -q
markers =
    unit: fast unit tests for local development
    slow: slow or integration tests (run on CI)
    e2e: end-to-end tests requiring running servers
testpaths =
    tests
